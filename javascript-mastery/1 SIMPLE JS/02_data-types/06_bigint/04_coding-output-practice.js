"use strict";

/*
Path: 1 SIMPLE JS\02_data-types\06_bigint\04_coding-output-practice.js
Topic: Bigint
Question bank: Bigint focused practice

How to study:
1. Read Q.
2. Hide Answer.
3. Predict output.
4. Then check Answer.
*/

// Q1. number math
{
  function run(a, b) {
    return a + b;
  }

  // Input:
  run(2, 3);

  // Answer:
  // 5
}

/////////////////////////////

// Q2. safe number check
{
  function run(value) {
    return Number.isFinite(value);
  }

  // Input:
  run(10);

  // Answer:
  // true
}

/////////////////////////////

// Q3. round number
{
  function run(value) {
    return Math.round(value);
  }

  // Input:
  run(10.6);

  // Answer:
  // 11
}

/////////////////////////////

// Q4. number math case 4
{
  function run(a, b) {
    return a + b;
  }

  // Input:
  run(2, 3);

  // Answer:
  // 5
}

/////////////////////////////

// Q5. safe number check case 5
{
  function run(value) {
    return Number.isFinite(value);
  }

  // Input:
  run(10);

  // Answer:
  // true
}

/////////////////////////////

// Q6. round number case 6
{
  function run(value) {
    return Math.round(value);
  }

  // Input:
  run(10.6);

  // Answer:
  // 11
}

/////////////////////////////

// Q7. number math case 7
{
  function run(a, b) {
    return a + b;
  }

  // Input:
  run(2, 3);

  // Answer:
  // 5
}

/////////////////////////////

// Q8. safe number check case 8
{
  function run(value) {
    return Number.isFinite(value);
  }

  // Input:
  run(10);

  // Answer:
  // true
}

/////////////////////////////

// Q9. round number case 9
{
  function run(value) {
    return Math.round(value);
  }

  // Input:
  run(10.6);

  // Answer:
  // 11
}

/////////////////////////////

// Q10. number math case 10
{
  function run(a, b) {
    return a + b;
  }

  // Input:
  run(2, 3);

  // Answer:
  // 5
}

/////////////////////////////

// Q11. safe number check case 11
{
  function run(value) {
    return Number.isFinite(value);
  }

  // Input:
  run(10);

  // Answer:
  // true
}

/////////////////////////////

// Q12. round number case 12
{
  function run(value) {
    return Math.round(value);
  }

  // Input:
  run(10.6);

  // Answer:
  // 11
}

/////////////////////////////

// Q13. number math case 13
{
  function run(a, b) {
    return a + b;
  }

  // Input:
  run(2, 3);

  // Answer:
  // 5
}

/////////////////////////////

// Q14. safe number check case 14
{
  function run(value) {
    return Number.isFinite(value);
  }

  // Input:
  run(10);

  // Answer:
  // true
}

/////////////////////////////

// Q15. round number case 15
{
  function run(value) {
    return Math.round(value);
  }

  // Input:
  run(10.6);

  // Answer:
  // 11
}

/////////////////////////////

// Q16. number math case 16
{
  function run(a, b) {
    return a + b;
  }

  // Input:
  run(2, 3);

  // Answer:
  // 5
}

/////////////////////////////

// Q17. safe number check case 17
{
  function run(value) {
    return Number.isFinite(value);
  }

  // Input:
  run(10);

  // Answer:
  // true
}

/////////////////////////////

// Q18. round number case 18
{
  function run(value) {
    return Math.round(value);
  }

  // Input:
  run(10.6);

  // Answer:
  // 11
}

/////////////////////////////

// Q19. number math case 19
{
  function run(a, b) {
    return a + b;
  }

  // Input:
  run(2, 3);

  // Answer:
  // 5
}

/////////////////////////////

// Q20. safe number check case 20
{
  function run(value) {
    return Number.isFinite(value);
  }

  // Input:
  run(10);

  // Answer:
  // true
}

/////////////////////////////

// Q21. round number case 21
{
  function run(value) {
    return Math.round(value);
  }

  // Input:
  run(10.6);

  // Answer:
  // 11
}

/////////////////////////////

// Q22. number math case 22
{
  function run(a, b) {
    return a + b;
  }

  // Input:
  run(2, 3);

  // Answer:
  // 5
}

/////////////////////////////

// Q23. safe number check case 23
{
  function run(value) {
    return Number.isFinite(value);
  }

  // Input:
  run(10);

  // Answer:
  // true
}

/////////////////////////////

// Q24. round number case 24
{
  function run(value) {
    return Math.round(value);
  }

  // Input:
  run(10.6);

  // Answer:
  // 11
}

/////////////////////////////

// Q25. number math case 25
{
  function run(a, b) {
    return a + b;
  }

  // Input:
  run(2, 3);

  // Answer:
  // 5
}

/////////////////////////////
