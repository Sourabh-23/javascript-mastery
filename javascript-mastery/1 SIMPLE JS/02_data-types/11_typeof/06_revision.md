# Typeof Revision

Path: `1 SIMPLE JS\02_data-types\11_typeof\06_revision.md`

## Must Remember

- typeof returns the type name as a string, with quirks like typeof null returning object.
- Always predict output before running code.
- Check whether the original value changes or a new value is returned.
- Practice one simple case and one edge case.

## Quick Example

```js
function run(value) {
  return value;
}
console.log(run("Typeof"));
```

## Study Order

1. Read `01_theory.md`.
2. Run `02_snippets.js`.
3. Predict answers in `04_coding-output-practice.js`.
4. Revise this file before interview.

## Interview Sentence

Typeof means: typeof returns the type name as a string, with quirks like typeof null returning object.
