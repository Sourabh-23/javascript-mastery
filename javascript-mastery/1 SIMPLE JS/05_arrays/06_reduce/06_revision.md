# Reduce Revision

Path: `1 SIMPLE JS\05_arrays\06_reduce\06_revision.md`

## Must Remember

- reduce converts an array into one final value like sum, object, or grouped data.
- Always predict output before running code.
- Check whether the original value changes or a new value is returned.
- Practice one simple case and one edge case.

## Quick Example

```js
const nums = [1, 2, 3];
const total = nums.reduce((sum, num) => sum + num, 0);
console.log(total);
```

## Study Order

1. Read `01_theory.md`.
2. Run `02_snippets.js`.
3. Predict answers in `04_coding-output-practice.js`.
4. Revise this file before interview.

## Interview Sentence

Reduce means: reduce converts an array into one final value like sum, object, or grouped data.
