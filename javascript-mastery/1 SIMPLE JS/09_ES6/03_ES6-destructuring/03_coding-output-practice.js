"use strict";

/*
Path: 1 SIMPLE JS\09_ES6\03_ES6-destructuring\03_coding-output-practice.js
Topic: Destructuring
Question bank: Destructuring focused practice

How to study:
1. Read Q.
2. Hide Answer.
3. Predict output.
4. Then check Answer.
*/

// Q1. array destructuring
{
  function run(arr) {
    const [first, second] = arr;
    return first + second;
  }

  // Input:
  run([1, 2]);

  // Answer:
  // 3
}

/////////////////////////////

// Q2. object destructuring
{
  function run(user) {
    const { name } = user;
    return name;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // "Amit"
}

/////////////////////////////

// Q3. default destructuring
{
  function run(user) {
    const { role = "user" } = user;
    return role;
  }

  // Input:
  run({});

  // Answer:
  // "user"
}

/////////////////////////////

// Q4. array destructuring case 4
{
  function run(arr) {
    const [first, second] = arr;
    return first + second;
  }

  // Input:
  run([1, 2]);

  // Answer:
  // 3
}

/////////////////////////////

// Q5. object destructuring case 5
{
  function run(user) {
    const { name } = user;
    return name;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // "Amit"
}

/////////////////////////////

// Q6. default destructuring case 6
{
  function run(user) {
    const { role = "user" } = user;
    return role;
  }

  // Input:
  run({});

  // Answer:
  // "user"
}

/////////////////////////////

// Q7. array destructuring case 7
{
  function run(arr) {
    const [first, second] = arr;
    return first + second;
  }

  // Input:
  run([1, 2]);

  // Answer:
  // 3
}

/////////////////////////////

// Q8. object destructuring case 8
{
  function run(user) {
    const { name } = user;
    return name;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // "Amit"
}

/////////////////////////////

// Q9. default destructuring case 9
{
  function run(user) {
    const { role = "user" } = user;
    return role;
  }

  // Input:
  run({});

  // Answer:
  // "user"
}

/////////////////////////////

// Q10. array destructuring case 10
{
  function run(arr) {
    const [first, second] = arr;
    return first + second;
  }

  // Input:
  run([1, 2]);

  // Answer:
  // 3
}

/////////////////////////////

// Q11. object destructuring case 11
{
  function run(user) {
    const { name } = user;
    return name;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // "Amit"
}

/////////////////////////////

// Q12. default destructuring case 12
{
  function run(user) {
    const { role = "user" } = user;
    return role;
  }

  // Input:
  run({});

  // Answer:
  // "user"
}

/////////////////////////////

// Q13. array destructuring case 13
{
  function run(arr) {
    const [first, second] = arr;
    return first + second;
  }

  // Input:
  run([1, 2]);

  // Answer:
  // 3
}

/////////////////////////////

// Q14. object destructuring case 14
{
  function run(user) {
    const { name } = user;
    return name;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // "Amit"
}

/////////////////////////////

// Q15. default destructuring case 15
{
  function run(user) {
    const { role = "user" } = user;
    return role;
  }

  // Input:
  run({});

  // Answer:
  // "user"
}

/////////////////////////////

// Q16. array destructuring case 16
{
  function run(arr) {
    const [first, second] = arr;
    return first + second;
  }

  // Input:
  run([1, 2]);

  // Answer:
  // 3
}

/////////////////////////////

// Q17. object destructuring case 17
{
  function run(user) {
    const { name } = user;
    return name;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // "Amit"
}

/////////////////////////////

// Q18. default destructuring case 18
{
  function run(user) {
    const { role = "user" } = user;
    return role;
  }

  // Input:
  run({});

  // Answer:
  // "user"
}

/////////////////////////////

// Q19. array destructuring case 19
{
  function run(arr) {
    const [first, second] = arr;
    return first + second;
  }

  // Input:
  run([1, 2]);

  // Answer:
  // 3
}

/////////////////////////////

// Q20. object destructuring case 20
{
  function run(user) {
    const { name } = user;
    return name;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // "Amit"
}

/////////////////////////////

// Q21. default destructuring case 21
{
  function run(user) {
    const { role = "user" } = user;
    return role;
  }

  // Input:
  run({});

  // Answer:
  // "user"
}

/////////////////////////////

// Q22. array destructuring case 22
{
  function run(arr) {
    const [first, second] = arr;
    return first + second;
  }

  // Input:
  run([1, 2]);

  // Answer:
  // 3
}

/////////////////////////////

// Q23. object destructuring case 23
{
  function run(user) {
    const { name } = user;
    return name;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // "Amit"
}

/////////////////////////////

// Q24. default destructuring case 24
{
  function run(user) {
    const { role = "user" } = user;
    return role;
  }

  // Input:
  run({});

  // Answer:
  // "user"
}

/////////////////////////////

// Q25. array destructuring case 25
{
  function run(arr) {
    const [first, second] = arr;
    return first + second;
  }

  // Input:
  run([1, 2]);

  // Answer:
  // 3
}

/////////////////////////////
