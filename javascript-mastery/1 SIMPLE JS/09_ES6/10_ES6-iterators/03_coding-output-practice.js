"use strict";

/*
Path: 1 SIMPLE JS\09_ES6\10_ES6-iterators\03_coding-output-practice.js
Topic: Iterators
Question bank: Iterators focused practice

How to study:
1. Read Q.
2. Hide Answer.
3. Predict output.
4. Then check Answer.
*/

// Q1. array iterator
{
  const iterator = [1, 2][Symbol.iterator]();

  // Input:
  iterator.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q2. iterator done
{
  const iterator = [1][Symbol.iterator]();
  iterator.next();

  // Input:
  iterator.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q3. array iterator case 3
{
  const iterator = [1, 2][Symbol.iterator]();

  // Input:
  iterator.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q4. iterator done case 4
{
  const iterator = [1][Symbol.iterator]();
  iterator.next();

  // Input:
  iterator.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q5. array iterator case 5
{
  const iterator = [1, 2][Symbol.iterator]();

  // Input:
  iterator.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q6. iterator done case 6
{
  const iterator = [1][Symbol.iterator]();
  iterator.next();

  // Input:
  iterator.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q7. array iterator case 7
{
  const iterator = [1, 2][Symbol.iterator]();

  // Input:
  iterator.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q8. iterator done case 8
{
  const iterator = [1][Symbol.iterator]();
  iterator.next();

  // Input:
  iterator.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q9. array iterator case 9
{
  const iterator = [1, 2][Symbol.iterator]();

  // Input:
  iterator.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q10. iterator done case 10
{
  const iterator = [1][Symbol.iterator]();
  iterator.next();

  // Input:
  iterator.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q11. array iterator case 11
{
  const iterator = [1, 2][Symbol.iterator]();

  // Input:
  iterator.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q12. iterator done case 12
{
  const iterator = [1][Symbol.iterator]();
  iterator.next();

  // Input:
  iterator.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q13. array iterator case 13
{
  const iterator = [1, 2][Symbol.iterator]();

  // Input:
  iterator.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q14. iterator done case 14
{
  const iterator = [1][Symbol.iterator]();
  iterator.next();

  // Input:
  iterator.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q15. array iterator case 15
{
  const iterator = [1, 2][Symbol.iterator]();

  // Input:
  iterator.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q16. iterator done case 16
{
  const iterator = [1][Symbol.iterator]();
  iterator.next();

  // Input:
  iterator.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q17. array iterator case 17
{
  const iterator = [1, 2][Symbol.iterator]();

  // Input:
  iterator.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q18. iterator done case 18
{
  const iterator = [1][Symbol.iterator]();
  iterator.next();

  // Input:
  iterator.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q19. array iterator case 19
{
  const iterator = [1, 2][Symbol.iterator]();

  // Input:
  iterator.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q20. iterator done case 20
{
  const iterator = [1][Symbol.iterator]();
  iterator.next();

  // Input:
  iterator.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q21. array iterator case 21
{
  const iterator = [1, 2][Symbol.iterator]();

  // Input:
  iterator.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q22. iterator done case 22
{
  const iterator = [1][Symbol.iterator]();
  iterator.next();

  // Input:
  iterator.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q23. array iterator case 23
{
  const iterator = [1, 2][Symbol.iterator]();

  // Input:
  iterator.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q24. iterator done case 24
{
  const iterator = [1][Symbol.iterator]();
  iterator.next();

  // Input:
  iterator.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q25. array iterator case 25
{
  const iterator = [1, 2][Symbol.iterator]();

  // Input:
  iterator.next().value;

  // Answer:
  // 1
}

/////////////////////////////
