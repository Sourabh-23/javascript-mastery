"use strict";

/*
Path: 1 SIMPLE JS\09_ES6\09_ES6-generators\04_ES6-coding-output-practice.js
Topic: Generators
Question bank: Generators focused practice

How to study:
1. Read Q.
2. Hide Answer.
3. Predict output.
4. Then check Answer.
*/

// Q1. generator first value
{
  function* numbers() {
    yield 1;
    yield 2;
  }
  const gen = numbers();

  // Input:
  gen.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q2. generator done
{
  function* numbers() {
    yield 1;
  }
  const gen = numbers();
  gen.next();

  // Input:
  gen.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q3. generator first value case 3
{
  function* numbers() {
    yield 1;
    yield 2;
  }
  const gen = numbers();

  // Input:
  gen.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q4. generator done case 4
{
  function* numbers() {
    yield 1;
  }
  const gen = numbers();
  gen.next();

  // Input:
  gen.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q5. generator first value case 5
{
  function* numbers() {
    yield 1;
    yield 2;
  }
  const gen = numbers();

  // Input:
  gen.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q6. generator done case 6
{
  function* numbers() {
    yield 1;
  }
  const gen = numbers();
  gen.next();

  // Input:
  gen.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q7. generator first value case 7
{
  function* numbers() {
    yield 1;
    yield 2;
  }
  const gen = numbers();

  // Input:
  gen.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q8. generator done case 8
{
  function* numbers() {
    yield 1;
  }
  const gen = numbers();
  gen.next();

  // Input:
  gen.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q9. generator first value case 9
{
  function* numbers() {
    yield 1;
    yield 2;
  }
  const gen = numbers();

  // Input:
  gen.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q10. generator done case 10
{
  function* numbers() {
    yield 1;
  }
  const gen = numbers();
  gen.next();

  // Input:
  gen.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q11. generator first value case 11
{
  function* numbers() {
    yield 1;
    yield 2;
  }
  const gen = numbers();

  // Input:
  gen.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q12. generator done case 12
{
  function* numbers() {
    yield 1;
  }
  const gen = numbers();
  gen.next();

  // Input:
  gen.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q13. generator first value case 13
{
  function* numbers() {
    yield 1;
    yield 2;
  }
  const gen = numbers();

  // Input:
  gen.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q14. generator done case 14
{
  function* numbers() {
    yield 1;
  }
  const gen = numbers();
  gen.next();

  // Input:
  gen.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q15. generator first value case 15
{
  function* numbers() {
    yield 1;
    yield 2;
  }
  const gen = numbers();

  // Input:
  gen.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q16. generator done case 16
{
  function* numbers() {
    yield 1;
  }
  const gen = numbers();
  gen.next();

  // Input:
  gen.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q17. generator first value case 17
{
  function* numbers() {
    yield 1;
    yield 2;
  }
  const gen = numbers();

  // Input:
  gen.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q18. generator done case 18
{
  function* numbers() {
    yield 1;
  }
  const gen = numbers();
  gen.next();

  // Input:
  gen.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q19. generator first value case 19
{
  function* numbers() {
    yield 1;
    yield 2;
  }
  const gen = numbers();

  // Input:
  gen.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q20. generator done case 20
{
  function* numbers() {
    yield 1;
  }
  const gen = numbers();
  gen.next();

  // Input:
  gen.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q21. generator first value case 21
{
  function* numbers() {
    yield 1;
    yield 2;
  }
  const gen = numbers();

  // Input:
  gen.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q22. generator done case 22
{
  function* numbers() {
    yield 1;
  }
  const gen = numbers();
  gen.next();

  // Input:
  gen.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q23. generator first value case 23
{
  function* numbers() {
    yield 1;
    yield 2;
  }
  const gen = numbers();

  // Input:
  gen.next().value;

  // Answer:
  // 1
}

/////////////////////////////

// Q24. generator done case 24
{
  function* numbers() {
    yield 1;
  }
  const gen = numbers();
  gen.next();

  // Input:
  gen.next().done;

  // Answer:
  // true
}

/////////////////////////////

// Q25. generator first value case 25
{
  function* numbers() {
    yield 1;
    yield 2;
  }
  const gen = numbers();

  // Input:
  gen.next().value;

  // Answer:
  // 1
}

/////////////////////////////
