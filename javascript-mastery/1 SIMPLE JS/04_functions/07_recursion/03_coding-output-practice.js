"use strict";

/*
Path: 1 SIMPLE JS\04_functions\07_recursion\03_coding-output-practice.js
Topic: Recursion
Question bank: Recursion focused practice

How to study:
1. Read Q.
2. Hide Answer.
3. Predict output.
4. Then check Answer.
*/

// Q1. recursive sum
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q2. base case
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q3. recursive sum case 3
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q4. base case case 4
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q5. recursive sum case 5
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q6. base case case 6
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q7. recursive sum case 7
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q8. base case case 8
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q9. recursive sum case 9
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q10. base case case 10
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q11. recursive sum case 11
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q12. base case case 12
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q13. recursive sum case 13
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q14. base case case 14
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q15. recursive sum case 15
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q16. base case case 16
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q17. recursive sum case 17
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q18. base case case 18
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q19. recursive sum case 19
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q20. base case case 20
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q21. recursive sum case 21
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q22. base case case 22
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q23. recursive sum case 23
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q24. base case case 24
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q25. recursive sum case 25
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// ================= PDF BASED PRACTICE =================

// PDF Q301. Write a JavaScript function for factorial and handle empty input.
{
  function forFactorialHandleEmpty(n) {
    if (n <= 1) return 1;
    return n * forFactorialHandleEmpty(n - 1);
  }

  // Input:
  forFactorialHandleEmpty(5);

  // Answer:
  // 120
}

/////////////////////////////

// PDF Q302. Solve a coding problem using fibonacci without using unnecessary built-in shortcuts.
{
  function fibonacciWithoutUnnecessaryBuiltIn(n) {
    const result = [0, 1];
    for (let i = 2; i < n; i++) result.push(result[i - 1] + result[i - 2]);
    return result.slice(0, n);
  }

  // Input:
  fibonacciWithoutUnnecessaryBuiltIn(6);

  // Answer:
  // [0, 1, 1, 2, 3, 5]
}

/////////////////////////////

// PDF Q310. Debug a broken JavaScript snippet related to power function and rewrite it correctly.
{
  function powerIt(...fns) {
    return (value) => fns.reduce((current, fn) => fn(current), value);
  }

  // Input:
  powerIt((n) => n + 1, (n) => n * 2)(3);

  // Answer:
  // 8
}

/////////////////////////////

// PDF Q311. Write a JavaScript function for factorial and handle empty input.
{
  function forFactorialHandleEmpty(n) {
    if (n <= 1) return 1;
    return n * forFactorialHandleEmpty(n - 1);
  }

  // Input:
  forFactorialHandleEmpty(5);

  // Answer:
  // 120
}

/////////////////////////////

// PDF Q312. Solve a coding problem using fibonacci without using unnecessary built-in shortcuts.
{
  function fibonacciWithoutUnnecessaryBuiltIn(n) {
    const result = [0, 1];
    for (let i = 2; i < n; i++) result.push(result[i - 1] + result[i - 2]);
    return result.slice(0, n);
  }

  // Input:
  fibonacciWithoutUnnecessaryBuiltIn(6);

  // Answer:
  // [0, 1, 1, 2, 3, 5]
}

/////////////////////////////

// PDF Q320. Debug a broken JavaScript snippet related to power function and rewrite it correctly.
{
  function powerIt(...fns) {
    return (value) => fns.reduce((current, fn) => fn(current), value);
  }

  // Input:
  powerIt((n) => n + 1, (n) => n * 2)(3);

  // Answer:
  // 8
}

/////////////////////////////

// PDF Q321. Write a JavaScript function for factorial and handle empty input.
{
  function forFactorialHandleEmpty(n) {
    if (n <= 1) return 1;
    return n * forFactorialHandleEmpty(n - 1);
  }

  // Input:
  forFactorialHandleEmpty(5);

  // Answer:
  // 120
}

/////////////////////////////

// PDF Q322. Solve a coding problem using fibonacci without using unnecessary built-in shortcuts.
{
  function fibonacciWithoutUnnecessaryBuiltIn(n) {
    const result = [0, 1];
    for (let i = 2; i < n; i++) result.push(result[i - 1] + result[i - 2]);
    return result.slice(0, n);
  }

  // Input:
  fibonacciWithoutUnnecessaryBuiltIn(6);

  // Answer:
  // [0, 1, 1, 2, 3, 5]
}

/////////////////////////////

// PDF Q330. Debug a broken JavaScript snippet related to power function and rewrite it correctly.
{
  function powerIt(...fns) {
    return (value) => fns.reduce((current, fn) => fn(current), value);
  }

  // Input:
  powerIt((n) => n + 1, (n) => n * 2)(3);

  // Answer:
  // 8
}

/////////////////////////////

// PDF Q331. Write a JavaScript function for factorial and handle empty input.
{
  function forFactorialHandleEmpty(n) {
    if (n <= 1) return 1;
    return n * forFactorialHandleEmpty(n - 1);
  }

  // Input:
  forFactorialHandleEmpty(5);

  // Answer:
  // 120
}

/////////////////////////////

// PDF Q332. Solve a coding problem using fibonacci without using unnecessary built-in shortcuts.
{
  function fibonacciWithoutUnnecessaryBuiltIn(n) {
    const result = [0, 1];
    for (let i = 2; i < n; i++) result.push(result[i - 1] + result[i - 2]);
    return result.slice(0, n);
  }

  // Input:
  fibonacciWithoutUnnecessaryBuiltIn(6);

  // Answer:
  // [0, 1, 1, 2, 3, 5]
}

/////////////////////////////

// PDF Q340. Debug a broken JavaScript snippet related to power function and rewrite it correctly.
{
  function powerIt(...fns) {
    return (value) => fns.reduce((current, fn) => fn(current), value);
  }

  // Input:
  powerIt((n) => n + 1, (n) => n * 2)(3);

  // Answer:
  // 8
}

/////////////////////////////

// PDF Q341. Write a JavaScript function for factorial and handle empty input.
{
  function forFactorialHandleEmpty(n) {
    if (n <= 1) return 1;
    return n * forFactorialHandleEmpty(n - 1);
  }

  // Input:
  forFactorialHandleEmpty(5);

  // Answer:
  // 120
}

/////////////////////////////

// PDF Q342. Solve a coding problem using fibonacci without using unnecessary built-in shortcuts.
{
  function fibonacciWithoutUnnecessaryBuiltIn(n) {
    const result = [0, 1];
    for (let i = 2; i < n; i++) result.push(result[i - 1] + result[i - 2]);
    return result.slice(0, n);
  }

  // Input:
  fibonacciWithoutUnnecessaryBuiltIn(6);

  // Answer:
  // [0, 1, 1, 2, 3, 5]
}

/////////////////////////////

// PDF Q350. Debug a broken JavaScript snippet related to power function and rewrite it correctly.
{
  function powerIt(...fns) {
    return (value) => fns.reduce((current, fn) => fn(current), value);
  }

  // Input:
  powerIt((n) => n + 1, (n) => n * 2)(3);

  // Answer:
  // 8
}

/////////////////////////////

