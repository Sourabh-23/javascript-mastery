"use strict";

/*
Path: 1 SIMPLE JS\04_functions\07_recursion\04_coding-output-practice.js
Topic: Recursion
Question bank: Recursion focused practice

How to study:
1. Read Q.
2. Hide Answer.
3. Predict output.
4. Then check Answer.
*/

// Q1. recursive sum
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q2. base case
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q3. recursive sum case 3
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q4. base case case 4
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q5. recursive sum case 5
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q6. base case case 6
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q7. recursive sum case 7
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q8. base case case 8
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q9. recursive sum case 9
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q10. base case case 10
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q11. recursive sum case 11
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q12. base case case 12
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q13. recursive sum case 13
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q14. base case case 14
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q15. recursive sum case 15
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q16. base case case 16
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q17. recursive sum case 17
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q18. base case case 18
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q19. recursive sum case 19
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q20. base case case 20
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q21. recursive sum case 21
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q22. base case case 22
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q23. recursive sum case 23
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////

// Q24. base case case 24
{
  function count(n) {
    if (n <= 0) return "stop";
    return count(n - 1);
  }

  // Input:
  count(2);

  // Answer:
  // "stop"
}

/////////////////////////////

// Q25. recursive sum case 25
{
  function sum(n) {
    if (n === 0) return 0;
    return n + sum(n - 1);
  }

  // Input:
  sum(3);

  // Answer:
  // 6
}

/////////////////////////////
