"use strict";

/*
Path: 1 SIMPLE JS\04_functions\08_currying\03_coding-output-practice.js
Topic: Currying
Question bank: Currying focused practice

How to study:
1. Read Q.
2. Hide Answer.
3. Predict output.
4. Then check Answer.
*/

// Q1. curried add
{
  function add(a) {
    return function (b) {
      return a + b;
    };
  }

  // Input:
  add(2)(3);

  // Answer:
  // 5
}

/////////////////////////////

// Q2. curried multiply
{
  const multiply = (a) => (b) => a * b;

  // Input:
  multiply(4)(5);

  // Answer:
  // 20
}

/////////////////////////////

// Q3. curried add case 3
{
  function add(a) {
    return function (b) {
      return a + b;
    };
  }

  // Input:
  add(2)(3);

  // Answer:
  // 5
}

/////////////////////////////

// Q4. curried multiply case 4
{
  const multiply = (a) => (b) => a * b;

  // Input:
  multiply(4)(5);

  // Answer:
  // 20
}

/////////////////////////////

// Q5. curried add case 5
{
  function add(a) {
    return function (b) {
      return a + b;
    };
  }

  // Input:
  add(2)(3);

  // Answer:
  // 5
}

/////////////////////////////

// Q6. curried multiply case 6
{
  const multiply = (a) => (b) => a * b;

  // Input:
  multiply(4)(5);

  // Answer:
  // 20
}

/////////////////////////////

// Q7. curried add case 7
{
  function add(a) {
    return function (b) {
      return a + b;
    };
  }

  // Input:
  add(2)(3);

  // Answer:
  // 5
}

/////////////////////////////

// Q8. curried multiply case 8
{
  const multiply = (a) => (b) => a * b;

  // Input:
  multiply(4)(5);

  // Answer:
  // 20
}

/////////////////////////////

// Q9. curried add case 9
{
  function add(a) {
    return function (b) {
      return a + b;
    };
  }

  // Input:
  add(2)(3);

  // Answer:
  // 5
}

/////////////////////////////

// Q10. curried multiply case 10
{
  const multiply = (a) => (b) => a * b;

  // Input:
  multiply(4)(5);

  // Answer:
  // 20
}

/////////////////////////////

// Q11. curried add case 11
{
  function add(a) {
    return function (b) {
      return a + b;
    };
  }

  // Input:
  add(2)(3);

  // Answer:
  // 5
}

/////////////////////////////

// Q12. curried multiply case 12
{
  const multiply = (a) => (b) => a * b;

  // Input:
  multiply(4)(5);

  // Answer:
  // 20
}

/////////////////////////////

// Q13. curried add case 13
{
  function add(a) {
    return function (b) {
      return a + b;
    };
  }

  // Input:
  add(2)(3);

  // Answer:
  // 5
}

/////////////////////////////

// Q14. curried multiply case 14
{
  const multiply = (a) => (b) => a * b;

  // Input:
  multiply(4)(5);

  // Answer:
  // 20
}

/////////////////////////////

// Q15. curried add case 15
{
  function add(a) {
    return function (b) {
      return a + b;
    };
  }

  // Input:
  add(2)(3);

  // Answer:
  // 5
}

/////////////////////////////

// Q16. curried multiply case 16
{
  const multiply = (a) => (b) => a * b;

  // Input:
  multiply(4)(5);

  // Answer:
  // 20
}

/////////////////////////////

// Q17. curried add case 17
{
  function add(a) {
    return function (b) {
      return a + b;
    };
  }

  // Input:
  add(2)(3);

  // Answer:
  // 5
}

/////////////////////////////

// Q18. curried multiply case 18
{
  const multiply = (a) => (b) => a * b;

  // Input:
  multiply(4)(5);

  // Answer:
  // 20
}

/////////////////////////////

// Q19. curried add case 19
{
  function add(a) {
    return function (b) {
      return a + b;
    };
  }

  // Input:
  add(2)(3);

  // Answer:
  // 5
}

/////////////////////////////

// Q20. curried multiply case 20
{
  const multiply = (a) => (b) => a * b;

  // Input:
  multiply(4)(5);

  // Answer:
  // 20
}

/////////////////////////////

// Q21. curried add case 21
{
  function add(a) {
    return function (b) {
      return a + b;
    };
  }

  // Input:
  add(2)(3);

  // Answer:
  // 5
}

/////////////////////////////

// Q22. curried multiply case 22
{
  const multiply = (a) => (b) => a * b;

  // Input:
  multiply(4)(5);

  // Answer:
  // 20
}

/////////////////////////////

// Q23. curried add case 23
{
  function add(a) {
    return function (b) {
      return a + b;
    };
  }

  // Input:
  add(2)(3);

  // Answer:
  // 5
}

/////////////////////////////

// Q24. curried multiply case 24
{
  const multiply = (a) => (b) => a * b;

  // Input:
  multiply(4)(5);

  // Answer:
  // 20
}

/////////////////////////////

// Q25. curried add case 25
{
  function add(a) {
    return function (b) {
      return a + b;
    };
  }

  // Input:
  add(2)(3);

  // Answer:
  // 5
}

/////////////////////////////
