"use strict";

/*
Path: 1 SIMPLE JS\01_variables\03_ES6-const\03_coding-output-practice.js
Topic: Const
Question bank: Const focused practice

How to study:
1. Read Q.
2. Hide Answer.
3. Predict output.
4. Then check Answer.
*/

// Q1. const needs value
{
  function run() {
    const role = "admin";
    return role;
  }

  // Input:
  run();

  // Answer:
  // "admin"
}

/////////////////////////////

// Q2. const object can mutate
{
  function run() {
    const user = { name: "Amit" };
    user.name = "Neha";
    return user.name;
  }

  // Input:
  run();

  // Answer:
  // "Neha"
}

/////////////////////////////

// Q3. const array can push
{
  function run() {
    const nums = [1, 2];
    nums.push(3);
    return nums;
  }

  // Input:
  run();

  // Answer:
  // [1, 2, 3]
}

/////////////////////////////

// Q4. const block scope
{
  function run() {
    const value = 1;
    if (true) {
      const value = 2;
    }
    return value;
  }

  // Input:
  run();

  // Answer:
  // 1
}

/////////////////////////////

// Q5. const for object reference
{
  function run() {
    const config = { dark: false };
    config.dark = true;
    return config.dark;
  }

  // Input:
  run();

  // Answer:
  // true
}

/////////////////////////////

// Q6. const needs value case 6
{
  function run() {
    const role = "admin";
    return role;
  }

  // Input:
  run();

  // Answer:
  // "admin"
}

/////////////////////////////

// Q7. const object can mutate case 7
{
  function run() {
    const user = { name: "Amit" };
    user.name = "Neha";
    return user.name;
  }

  // Input:
  run();

  // Answer:
  // "Neha"
}

/////////////////////////////

// Q8. const array can push case 8
{
  function run() {
    const nums = [1, 2];
    nums.push(3);
    return nums;
  }

  // Input:
  run();

  // Answer:
  // [1, 2, 3]
}

/////////////////////////////

// Q9. const block scope case 9
{
  function run() {
    const value = 1;
    if (true) {
      const value = 2;
    }
    return value;
  }

  // Input:
  run();

  // Answer:
  // 1
}

/////////////////////////////

// Q10. const for object reference case 10
{
  function run() {
    const config = { dark: false };
    config.dark = true;
    return config.dark;
  }

  // Input:
  run();

  // Answer:
  // true
}

/////////////////////////////

// Q11. const needs value case 11
{
  function run() {
    const role = "admin";
    return role;
  }

  // Input:
  run();

  // Answer:
  // "admin"
}

/////////////////////////////

// Q12. const object can mutate case 12
{
  function run() {
    const user = { name: "Amit" };
    user.name = "Neha";
    return user.name;
  }

  // Input:
  run();

  // Answer:
  // "Neha"
}

/////////////////////////////

// Q13. const array can push case 13
{
  function run() {
    const nums = [1, 2];
    nums.push(3);
    return nums;
  }

  // Input:
  run();

  // Answer:
  // [1, 2, 3]
}

/////////////////////////////

// Q14. const block scope case 14
{
  function run() {
    const value = 1;
    if (true) {
      const value = 2;
    }
    return value;
  }

  // Input:
  run();

  // Answer:
  // 1
}

/////////////////////////////

// Q15. const for object reference case 15
{
  function run() {
    const config = { dark: false };
    config.dark = true;
    return config.dark;
  }

  // Input:
  run();

  // Answer:
  // true
}

/////////////////////////////

// Q16. const needs value case 16
{
  function run() {
    const role = "admin";
    return role;
  }

  // Input:
  run();

  // Answer:
  // "admin"
}

/////////////////////////////

// Q17. const object can mutate case 17
{
  function run() {
    const user = { name: "Amit" };
    user.name = "Neha";
    return user.name;
  }

  // Input:
  run();

  // Answer:
  // "Neha"
}

/////////////////////////////

// Q18. const array can push case 18
{
  function run() {
    const nums = [1, 2];
    nums.push(3);
    return nums;
  }

  // Input:
  run();

  // Answer:
  // [1, 2, 3]
}

/////////////////////////////

// Q19. const block scope case 19
{
  function run() {
    const value = 1;
    if (true) {
      const value = 2;
    }
    return value;
  }

  // Input:
  run();

  // Answer:
  // 1
}

/////////////////////////////

// Q20. const for object reference case 20
{
  function run() {
    const config = { dark: false };
    config.dark = true;
    return config.dark;
  }

  // Input:
  run();

  // Answer:
  // true
}

/////////////////////////////

// Q21. const needs value case 21
{
  function run() {
    const role = "admin";
    return role;
  }

  // Input:
  run();

  // Answer:
  // "admin"
}

/////////////////////////////

// Q22. const object can mutate case 22
{
  function run() {
    const user = { name: "Amit" };
    user.name = "Neha";
    return user.name;
  }

  // Input:
  run();

  // Answer:
  // "Neha"
}

/////////////////////////////

// Q23. const array can push case 23
{
  function run() {
    const nums = [1, 2];
    nums.push(3);
    return nums;
  }

  // Input:
  run();

  // Answer:
  // [1, 2, 3]
}

/////////////////////////////

// Q24. const block scope case 24
{
  function run() {
    const value = 1;
    if (true) {
      const value = 2;
    }
    return value;
  }

  // Input:
  run();

  // Answer:
  // 1
}

/////////////////////////////

// Q25. const for object reference case 25
{
  function run() {
    const config = { dark: false };
    config.dark = true;
    return config.dark;
  }

  // Input:
  run();

  // Answer:
  // true
}

/////////////////////////////
