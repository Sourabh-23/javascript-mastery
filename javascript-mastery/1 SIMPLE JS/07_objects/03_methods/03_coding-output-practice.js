"use strict";

/*
Path: 1 SIMPLE JS\07_objects\03_methods\03_coding-output-practice.js
Topic: Methods
Question bank: Methods focused practice

How to study:
1. Read Q.
2. Hide Answer.
3. Predict output.
4. Then check Answer.
*/

// Q1. read property
{
  function run(user) {
    return user.name;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // "Amit"
}

/////////////////////////////

// Q2. add property
{
  function run(user) {
    user.active = true;
    return user;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // { name: "Amit", active: true }
}

/////////////////////////////

// Q3. object keys
{
  function run(obj) {
    return Object.keys(obj);
  }

  // Input:
  run({ a: 1, b: 2 });

  // Answer:
  // ["a", "b"]
}

/////////////////////////////

// Q4. object values
{
  function run(obj) {
    return Object.values(obj);
  }

  // Input:
  run({ a: 1, b: 2 });

  // Answer:
  // [1, 2]
}

/////////////////////////////

// Q5. destructure
{
  function run(user) {
    const { name } = user;
    return name;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // "Amit"
}

/////////////////////////////

// Q6. spread copy
{
  function run(user) {
    return { ...user, role: "dev" };
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // { name: "Amit", role: "dev" }
}

/////////////////////////////

// Q7. read property case 7
{
  function run(user) {
    return user.name;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // "Amit"
}

/////////////////////////////

// Q8. add property case 8
{
  function run(user) {
    user.active = true;
    return user;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // { name: "Amit", active: true }
}

/////////////////////////////

// Q9. object keys case 9
{
  function run(obj) {
    return Object.keys(obj);
  }

  // Input:
  run({ a: 1, b: 2 });

  // Answer:
  // ["a", "b"]
}

/////////////////////////////

// Q10. object values case 10
{
  function run(obj) {
    return Object.values(obj);
  }

  // Input:
  run({ a: 1, b: 2 });

  // Answer:
  // [1, 2]
}

/////////////////////////////

// Q11. destructure case 11
{
  function run(user) {
    const { name } = user;
    return name;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // "Amit"
}

/////////////////////////////

// Q12. spread copy case 12
{
  function run(user) {
    return { ...user, role: "dev" };
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // { name: "Amit", role: "dev" }
}

/////////////////////////////

// Q13. read property case 13
{
  function run(user) {
    return user.name;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // "Amit"
}

/////////////////////////////

// Q14. add property case 14
{
  function run(user) {
    user.active = true;
    return user;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // { name: "Amit", active: true }
}

/////////////////////////////

// Q15. object keys case 15
{
  function run(obj) {
    return Object.keys(obj);
  }

  // Input:
  run({ a: 1, b: 2 });

  // Answer:
  // ["a", "b"]
}

/////////////////////////////

// Q16. object values case 16
{
  function run(obj) {
    return Object.values(obj);
  }

  // Input:
  run({ a: 1, b: 2 });

  // Answer:
  // [1, 2]
}

/////////////////////////////

// Q17. destructure case 17
{
  function run(user) {
    const { name } = user;
    return name;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // "Amit"
}

/////////////////////////////

// Q18. spread copy case 18
{
  function run(user) {
    return { ...user, role: "dev" };
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // { name: "Amit", role: "dev" }
}

/////////////////////////////

// Q19. read property case 19
{
  function run(user) {
    return user.name;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // "Amit"
}

/////////////////////////////

// Q20. add property case 20
{
  function run(user) {
    user.active = true;
    return user;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // { name: "Amit", active: true }
}

/////////////////////////////

// Q21. object keys case 21
{
  function run(obj) {
    return Object.keys(obj);
  }

  // Input:
  run({ a: 1, b: 2 });

  // Answer:
  // ["a", "b"]
}

/////////////////////////////

// Q22. object values case 22
{
  function run(obj) {
    return Object.values(obj);
  }

  // Input:
  run({ a: 1, b: 2 });

  // Answer:
  // [1, 2]
}

/////////////////////////////

// Q23. destructure case 23
{
  function run(user) {
    const { name } = user;
    return name;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // "Amit"
}

/////////////////////////////

// Q24. spread copy case 24
{
  function run(user) {
    return { ...user, role: "dev" };
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // { name: "Amit", role: "dev" }
}

/////////////////////////////

// Q25. read property case 25
{
  function run(user) {
    return user.name;
  }

  // Input:
  run({ name: "Amit" });

  // Answer:
  // "Amit"
}

/////////////////////////////
